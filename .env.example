# Supabase — copy to .env.local and fill in values from:
# Supabase Dashboard → Project Settings → API Keys

NEXT_PUBLIC_SUPABASE_URL=https://rcwasdxhklmzfqurakht.supabase.co

# Use the "Secret keys" → default key (sb_secret_...). Reveal & copy it.
# Server-side only — never expose in the browser.
SUPABASE_SERVICE_ROLE_KEY=sb_secret_your-key-here
